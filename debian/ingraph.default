# Defaults for ingraph initscript
# sourced by /etc/init.d/ingraph
# installed at /etc/default/ingraph by the maintainer scripts

# Options for ingraphd
INGRAPHD_OPTS="-o /var/log/ingraph/ingraphd.log"

# Options for ingraph-collectord
INGRAPHCOLLECTORD_OPTS="-o /var/log/ingraph/ingraph-collectord.log -P /var/spool/ingraph/perfdata -m REMOVE"

